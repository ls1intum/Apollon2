export const DEFAULT_FONT =
  "400 16px Inter, system-ui, Avenir, Helvetica, Arial, sans-serif"
export const DEFAULT_HEADER_HEIGHT = 40
export const DEFAULT_HEADER_HEIGHT_WITH_STREOTYPE = 50
export const DEFAULT_ATTRIBUTE_HEIGHT = 30
export const DEFAULT_METHOD_HEIGHT = 30
export const DEFAULT_PADDING = 10
export const LINE_WIDTH = 1
export const LINE_WIDTH_ON_EDGE = LINE_WIDTH * 2 // This is double of the line width because svg stroke is centered on the edge and we want to have the same linewidth in the inner side of the edge
