{
  "name": "@apollon2/server",
  "version": "1.0.0",
  "dependencies": {
    "ws": "8.18.1",
    "y-websocket": "1.5.0"
  },
  "scripts": {
    "build": "echo bypass building in server because it is js now",
    "start": "HOST=localhost PORT=3000 YPERSISTENCE=./dbDir node src/server.js",
    "lint": "echo bypass  lint in server"
  }
}
