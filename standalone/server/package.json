{
  "name": "@apollon2/server",
  "version": "1.0.0",
  "dependencies": {
    "@apollon2/library": "^0.0.1",
    "@apollon2/webapp": "^0.0.1",
    "react-router": "^7.3.0",
    "ws": "^8.0.0",
    "y-websocket": "^1.5.0",
    "zustand": "^5.0.3"
  },
  "scripts": {
    "build": "echo bypass building in server because it is js now",
    "start": "node src/server.js",
    "lint": "echo bypass  lint in server"
  }
}
